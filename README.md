## Mine Sweeper in the terminal

# Installation

In the poject directory run the following commands to install the app. I will be named term-minsweeper.

```bash
cmake -B out
cmake --build out
sudo cmake --install out
```

The game only works for unix like systems.

# Usage

See 'term-minsweeper --help' for help.


